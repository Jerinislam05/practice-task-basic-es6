// write an arrow function that will take 3 parameters, will multiply the parameters and return the result.

const multiply = (x, y, z) => x * y * z;

const result = multiply(3, 4, 2);
console.log(result); // answer: 24;
